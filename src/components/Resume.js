import '../Navigation.css';

export default function Resume() {
    //logic needed for resume

    return (
        <div className='actual-content'>
            resume content
        </div>

    );
}